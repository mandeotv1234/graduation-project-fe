name: Deploy Frontend

on:
  push:
    branches:
      - feature/GRAD-50-Implement-changes-related-to-APIs-in-the-specification-for-the-frontend

env:
  REGISTRY: registry.digitalocean.com/graduation
  IMAGE_NAME: frontend

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Build NextJS
        env:
          NEXT_PUBLIC_API_URL: ${{ secrets.NEXT_PUBLIC_API_URL }}
        run: npm run build

      # login DO registry
      - name: Login DigitalOcean Registry
        uses: docker/login-action@v3
        with:
          registry: registry.digitalocean.com
          username: ${{ secrets.DO_REGISTRY_TOKEN }}
          password: ${{ secrets.DO_REGISTRY_TOKEN }}

      # build image
      - name: Build Docker Image
        run: |
          docker build \
            --build-arg NEXT_PUBLIC_API_URL=${{ secrets.NEXT_PUBLIC_API_URL }} \
            -t $REGISTRY/$IMAGE_NAME:latest .

      # push image
      - name: Push Image
        run: |
          docker push $REGISTRY/$IMAGE_NAME:latest

      # deploy droplet
      - name: Deploy to Droplet
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: root
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            docker login registry.digitalocean.com \
              -u ${{ secrets.DO_REGISTRY_TOKEN }} \
              -p ${{ secrets.DO_REGISTRY_TOKEN }}

            cd /root/app

            docker-compose pull frontend
            docker-compose up -d frontend
